<pages:PageBase
    x:Class="Rightpoint.Peeps.Client.Pages.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:pages="using:Rightpoint.Peeps.Client.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <FlipView ItemsSource="{Binding Path=Peeps, Mode=OneWay}">
            <FlipView.ItemTemplate>
                <DataTemplate>
                    <RelativePanel>
                        <Image x:Name="peepsImage" Source="{Binding Path=ImageBytes, Converter={StaticResource ByteArrayToImageConverter}}"/>
                        <StackPanel RelativePanel.RightOf="peepsImage" Padding="15,10">
                            <TextBlock Text="{Binding Name}" FontSize="32"/>
                            <TextBlock Text="{Binding Office}" FontSize="32"/>
                            <TextBlock Text="{Binding Team}" FontSize="32"/>
                            <TextBlock Text="{Binding Hometown}" FontSize="32"/>
                        </StackPanel>
                    </RelativePanel>
                </DataTemplate>
            </FlipView.ItemTemplate>
        </FlipView>
    </Grid>
</pages:PageBase>
